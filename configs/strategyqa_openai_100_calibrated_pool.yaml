run_name: "strategyqa_openai_100_calibrated_shared"
dataset_path: "data/prepared/strategyqa_100.jsonl"
output_dir: "data/runs"
task_profile: "strategyqa"

model:
  provider: "openai"
  name: "gpt-5.4-mini"
  prompt_id: "strategyqa_cot_steps_v1"
  temperature: 0.2
  max_output_tokens: 800

scoring:
  risk_threshold: 0.20
  improvement_threshold: 0.02
  max_iters: 2
  risk_formula: "weighted"
  verifier_weight: 0.75
  contradiction_weight: 0.25

repair_acceptance:
  mode: "risk_only"
  support_tolerance: 0.05
  max_regression_risk: 0.35
